# ══════════════════════════════════════════════════════════════
#  Jenkins Configuration as Code (JCasC)
#  Replaces ALL manual Phase 3 configuration:
#    - Credentials (github-creds, github-token, ACCOUNT_ID, etc.)
#    - SonarQube server configuration
#    - Pipeline job definitions
#    - Security settings
#
#  Sensitive values are injected via environment variables.
#  Set these BEFORE Jenkins starts:
#    GITHUB_USERNAME, GITHUB_PAT, AWS_ACCOUNT_ID, SONARQUBE_TOKEN
# ══════════════════════════════════════════════════════════════

jenkins:
  systemMessage: "Cloud-Native EKS Project — Configured via JCasC"

  # Security — create admin user
  securityRealm:
    local:
      allowsSignup: false
      users:
        - id: "admin"
          password: "${JENKINS_ADMIN_PASSWORD:-admin123}"

  authorizationStrategy:
    loggedInUsersCanDoAnything:
      allowAnonymousRead: false

  # Number of executors on the built-in node
  numExecutors: 2

# ──────────────────────────────────────────────
#  Credentials
# ──────────────────────────────────────────────
credentials:
  system:
    domainCredentials:
      - credentials:
          # GitHub — Username with password (for SCM checkout)
          - usernamePassword:
              scope: GLOBAL
              id: "github-creds"
              description: "GitHub credentials for SCM checkout"
              username: "${GITHUB_USERNAME}"
              password: "${GITHUB_PAT}"

          # GitHub — Secret text (for pipeline git push)
          - string:
              scope: GLOBAL
              id: "github-token"
              description: "GitHub PAT for pipeline git push"
              secret: "${GITHUB_PAT}"

          # AWS Account ID
          - string:
              scope: GLOBAL
              id: "ACCOUNT_ID"
              description: "AWS Account ID"
              secret: "${AWS_ACCOUNT_ID}"

          # SonarQube token
          - string:
              scope: GLOBAL
              id: "sonar"
              description: "SonarQube authentication token"
              secret: "${SONARQUBE_TOKEN}"

# ──────────────────────────────────────────────
#  SonarQube Server Configuration
# ──────────────────────────────────────────────
unclassified:
  sonarGlobalConfiguration:
    buildWrapperEnabled: true
    installations:
      - name: "sonar"
        serverUrl: "http://localhost:9000"
        credentialsId: "sonar"
        triggers:
          skipScmCause: false
          skipUpstreamCause: false

# ──────────────────────────────────────────────
#  Pipeline Job Definitions
# ──────────────────────────────────────────────
jobs:
  # ── Nimbus service pipelines (all share one Jenkinsfile, default SERVICE_NAME differs) ──

  - script: >
      pipelineJob('nimbus-auth-service') {
        description('auth-service — SonarQube + Trivy + ECR + Helm values update')
        parameters {
          choiceParam('SERVICE_NAME',
            ['auth-service', 'catalog-service', 'cart-service', 'order-service', 'notification-service'],
            'NimbusRetail service to build and deploy')
        }
        definition {
          cpsScm {
            scm {
              git {
                remote {
                  url('https://github.com/ibrahim-2010/cloud-native-eks.git')
                  credentials('github-creds')
                }
                branches('*/main')
              }
            }
            scriptPath('Jenkins-Pipeline-Code/Jenkinsfile-Nimbus')
          }
        }
      }

  - script: >
      pipelineJob('nimbus-catalog-service') {
        description('catalog-service — SonarQube + Trivy + ECR + Helm values update')
        parameters {
          choiceParam('SERVICE_NAME',
            ['catalog-service', 'auth-service', 'cart-service', 'order-service', 'notification-service'],
            'NimbusRetail service to build and deploy')
        }
        definition {
          cpsScm {
            scm {
              git {
                remote {
                  url('https://github.com/ibrahim-2010/cloud-native-eks.git')
                  credentials('github-creds')
                }
                branches('*/main')
              }
            }
            scriptPath('Jenkins-Pipeline-Code/Jenkinsfile-Nimbus')
          }
        }
      }

  - script: >
      pipelineJob('nimbus-cart-service') {
        description('cart-service — SonarQube + Trivy + ECR + Helm values update')
        parameters {
          choiceParam('SERVICE_NAME',
            ['cart-service', 'auth-service', 'catalog-service', 'order-service', 'notification-service'],
            'NimbusRetail service to build and deploy')
        }
        definition {
          cpsScm {
            scm {
              git {
                remote {
                  url('https://github.com/ibrahim-2010/cloud-native-eks.git')
                  credentials('github-creds')
                }
                branches('*/main')
              }
            }
            scriptPath('Jenkins-Pipeline-Code/Jenkinsfile-Nimbus')
          }
        }
      }

  - script: >
      pipelineJob('nimbus-order-service') {
        description('order-service — SonarQube + Trivy + ECR + Helm values update')
        parameters {
          choiceParam('SERVICE_NAME',
            ['order-service', 'auth-service', 'catalog-service', 'cart-service', 'notification-service'],
            'NimbusRetail service to build and deploy')
        }
        definition {
          cpsScm {
            scm {
              git {
                remote {
                  url('https://github.com/ibrahim-2010/cloud-native-eks.git')
                  credentials('github-creds')
                }
                branches('*/main')
              }
            }
            scriptPath('Jenkins-Pipeline-Code/Jenkinsfile-Nimbus')
          }
        }
      }

  - script: >
      pipelineJob('nimbus-notification-service') {
        description('notification-service — SonarQube + Trivy + ECR + Helm values update')
        parameters {
          choiceParam('SERVICE_NAME',
            ['notification-service', 'auth-service', 'catalog-service', 'cart-service', 'order-service'],
            'NimbusRetail service to build and deploy')
        }
        definition {
          cpsScm {
            scm {
              git {
                remote {
                  url('https://github.com/ibrahim-2010/cloud-native-eks.git')
                  credentials('github-creds')
                }
                branches('*/main')
              }
            }
            scriptPath('Jenkins-Pipeline-Code/Jenkinsfile-Nimbus')
          }
        }
      }

  # ── Infrastructure pipeline — deploys EKS cluster + full platform ──
  - script: >
      pipelineJob('nimbus-infrastructure') {
        description('Deploy EKS cluster + RDS + Redis + Kafka + ESO + Kyverno + observability + ArgoCD')
        definition {
          cpsScm {
            scm {
              git {
                remote {
                  url('https://github.com/ibrahim-2010/cloud-native-eks.git')
                  credentials('github-creds')
                }
                branches('*/main')
              }
            }
            scriptPath('Jenkins-Pipeline-Code/Jenkinsfile-Infrastructure')
          }
        }
      }
