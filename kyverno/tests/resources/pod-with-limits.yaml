apiVersion: v1
kind: Pod
metadata:
  name: pod-with-limits
  namespace: nimbus
  labels:
    app: test-service
spec:
  containers:
    - name: app
      image: nginx:1.27.0
      resources:
        requests:
          cpu: "100m"
          memory: "128Mi"
        limits:
          cpu: "300m"
          memory: "256Mi"
